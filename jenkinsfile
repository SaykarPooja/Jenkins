pipeline {
    agent any

    tools {
        maven 'Maven3'   // Name of Maven installation in Jenkins
        jdk 'Java17'     // Name of JDK installation in Jenkins
    }

    environment {
        // SonarQube URL and token stored in Jenkins Credentials (type: Secret Text)
        SONAR_HOST_URL = 'http://52.66.205.151:9000'
        SONAR_AUTH_TOKEN = credentials('sonar-token')  // replace with your Jenkins secret ID
    }

    stages {

        stage('Checkout Code') {
            steps {
                git branch: 'main', url: 'https://github.com/SaykarPooja/Jenkins.git'
            }
        }

        stage('Build with Maven') {
            steps {
                sh 'mvn clean install'
            }
        }

        stage('SonarQube Analysis') {
            steps {
                sh """
                    mvn sonar:sonar \
                    -Dsonar.projectKey=MyProject \
                    -Dsonar.host.url=$SONAR_HOST_URL \
                    -Dsonar.login=$SONAR_AUTH_TOKEN
                """
            }
        }

    }

    post {
        success {
            echo 'Build & SonarQube Analysis Completed Successfully!'
        }
        failure {
            echo 'Build or Analysis Failed!'
        }
    }
}
